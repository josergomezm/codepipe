# Copy this file to `.env.local` (for `npm run dev`) or `.env.remote`
# (for `npm run dev:remote`) and fill in your own values.
#
# Remote access over Tailscale. Find your hostname with `tailscale status`.
# Leave blank for local-only development.
TAILSCALE_HOST=
TAILSCALE_PORT=443
